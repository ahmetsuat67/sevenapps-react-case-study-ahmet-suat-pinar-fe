// src/app/layout.tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Live Markdown Playground",
  description:
    "Live Markdown editor with real-time preview, theme toggle, and IndexedDB persistence.",
  keywords: ["markdown", "editor", "nextjs", "react", "dexie", "tailwind"],
  authors: [
    {
      name: "Ahmet Suat PINAR",
      url: "https://your-website.example.com",
    },
  ],
  creator: "Ahmet Suat PINAR",
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#ffffff" },
    { media: "(prefers-color-scheme: dark)", color: "#1f2937" },
  ],
  icons: {
    icon: "/markdown-svg-icon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        {children}
      </body>
    </html>
  );
}
